# Harness placeholder

This directory is a placeholder for runtime/harness integration (assembler, compiler, web runtime).
Place scripts or tool configs here, e.g. `harness/build.sh` or `harness/docker/`.
